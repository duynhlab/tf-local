# Copy to terraform.tfvars (gitignored) for real AWS bootstrap apply.

aws_region          = "ap-southeast-1"
state_bucket_name   = "YOUR-UNIQUE-terraform-state"
# state_kms_key_arn = "arn:aws:kms:ap-southeast-1:ACCOUNT:key/KEY-ID"
